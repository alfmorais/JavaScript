const numbers = [0, 1, 2, 3];
const coordenates = [0.458787, 0.458789];
const quantityNumbers = numbers.length;
const quantityCoordenates = coordenates.length;

console.log("Quantidade total de elementos no array numbers: " + quantityNumbers);
console.log("Quantidade total de elementos no array coordenates: " + quantityCoordenates);
